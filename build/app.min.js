(function() {
  d3.csv('data/major_projects.csv', function(data) {
    var container;
    console.log("Got data", data);
    container = d3.select('#viz-container');
    return container.text("awesome viz here");
  });

}).call(this);
